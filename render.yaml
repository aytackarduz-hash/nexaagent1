services:
  - type: web
    name: nexa-ultra-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 300
    envVars:
      - key: GEMINI_API_KEY
        sync: false
